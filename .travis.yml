language: r
warnings_are_errors: false
sudo: required
compiler:
  - gcc
  - clang
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
    - clang
env:
 global:
   - CRAN: http://cran.rstudio.com
r_packages:
   - testthat
   - knitr
   - Rcpp

